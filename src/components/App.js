import './App.css';
import ButtonPad from './ButtonPad';

function App() {
  // const handleButtonClick = (value) => {
  //   setInputValue(prevValue => prevValue + value);
  // };
  return (
    <div className="App">
      <ButtonPad/>
    </div>
  );
}

export default App;
